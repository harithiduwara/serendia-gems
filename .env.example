# Public site origin — used for canonical URLs, sitemap, and OG tags.
NEXT_PUBLIC_SITE_URL=https://serendiagems.com

# Enquiry delivery (server-only; never exposed to the browser).
# Leave unset in development: enquiries are logged to the server console instead.
ENQUIRY_TO_EMAIL=
RESEND_API_KEY=
